export interface Settings {
  notificationType: {
    RESERVATION_CREATED?: boolean,
    RESERVATION_CANCELED?: boolean,
    NEW_USER_RATING?: boolean,
    NEW_ACCOMMODATION_RATING?: boolean,
    RESERVATION_RESPONSE?: boolean
  }
}
